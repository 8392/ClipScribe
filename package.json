{
  "name": "clipscribe",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "dev:api": "bun run --filter @clipscribe/api dev",
    "dev:web": "bun run --filter @clipscribe/web dev",
    "build": "bun run --filter @clipscribe/shared build && bun run --filter @clipscribe/api build && bun run --filter @clipscribe/web build",
    "lint": "eslint .",
    "typecheck": "bun run --filter @clipscribe/shared build && bun run --filter @clipscribe/api typecheck && bun run --filter @clipscribe/web typecheck",
    "test:e2e": "bun run scripts/test-e2e.ts"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.12.0",
    "eslint": "^9.24.0",
    "typescript": "^5.8.3"
  }
}
